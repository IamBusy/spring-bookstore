# Server configuration
server:
  port: 8090
  contextPath:

#
jms:
  orders:
    create: orders.process.create

# Spring configuration
spring:
  jackson:
    serialization:
      INDENT_OUTPUT: true

  data:
    mongodb:
      host: localhost
      port: 27017
      database: bookstore


  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
    packages:
      trust-all: true
  redis:
    host: 127.0.0.1
    database: 0
    port: 6379
    timeout: 0






